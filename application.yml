spring:
  profiles:
    active:
      dev

---
spring:
  profiles:
    application:
      name: microservicecloud-config-dev

---
spring:
  profiles:
    application:
      name: microservicecloud-config-test